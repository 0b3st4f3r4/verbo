# Dependências de desenvolvimento da suíte de testes da Etapa 1
# (PLAN.md §1.3 — suíte de regressão contínua; AGENTS.md §3: behave para BDD, pytest para TDD)
behave>=1.3.3
pytest>=8.0
pytest-cov>=6.0
PyYAML>=6.0
# Framework de hooks git (.pre-commit-config.yaml): o CI inteiro antes do commit
pre-commit>=4.0
